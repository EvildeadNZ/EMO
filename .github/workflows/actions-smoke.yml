name: Actions write-permission test

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  permission_test:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm job can start with write permission
        run: echo "Write-permission workflow started"
